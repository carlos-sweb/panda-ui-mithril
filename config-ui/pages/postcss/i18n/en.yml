title: Postcss
intro: CSS generation pipeline of the project — Panda is one layer; PostCSS plugins process its output (unused token variables pruning, url copy, nesting, autoprefixer, minification).
loadingCatalog: Loading the official plugin list…
errorPrefix: 'Error: '
tabs:
  install: Install
  available: Available
install:
  intro: Search the official PostCSS plugin list — “Install” runs bun add in your project, so the plugin becomes available in node_modules. Wiring it into the CSS pipeline is a later step.
  placeholder: Search plugins… (e.g. import, preset-env, nested)
  noResults: 'No plugins match "{query}".'
  category: '{category}'
  install: Install
  installing: Installing…
  installed: installed
  installedHint: '{name} installed as {pkg} — it is now available in node_modules.'
  seeMore: See plugin page
available:
  intro: Plugins from the official catalog present in this project (node_modules). Wiring them into the pipeline comes next.
  empty: No catalog plugins in node_modules yet — use the Install tab.
  loading: Loading installed plugins…
  remove: Remove package
  removing: Removing…
  removed: '{pkg} removed.'
  npm: 'npm: {pkg}'
